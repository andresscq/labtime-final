# URL del reverse proxy (nginx) del backend LabTime
VITE_API_BASE_URL=http://localhost:9090

# Configuracion de AWS Cognito (App client tipo SPA, sin client secret)
VITE_COGNITO_DOMAIN=https://labtime.auth.us-east-1.amazoncognito.com
VITE_COGNITO_CLIENT_ID=1kr72473ofc7lafpri19iq6mkc
VITE_COGNITO_REGION=us-east-1

# Redirect URI registrada en el App client de Cognito.
# En web (dev): http://localhost:5173/callback
# En la app empaquetada (Capacitor): com.pucetec.labtime://callback
VITE_REDIRECT_URI=http://localhost:5173/callback
